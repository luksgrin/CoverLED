<resources>
    <style name="Theme.CoverLED" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!-- Pure black, no system bars: only the dot pixels emit light on the cover OLED. -->
    <style name="Theme.CoverLED.Indicator" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
